# GVHMR Docker Configuration

# Web Interface Port
PORT=5000

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# Upload Settings
MAX_UPLOAD_SIZE_MB=500

# Processing Settings
PROCESSING_TIMEOUT_SECONDS=600

# GPU Settings (handled by docker-compose.yml)
# Ensure nvidia-docker2 is installed on the host system
